<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <nav>
        <h2>Workoutxpress</h2>
        <span><a class="homePage" href="/workoutxpress">Home</a></span>
        <span><a class="newWorkout" href="/workoutxpress/new">Create</a></span>
        <span><a class="pastWorkout" href="/workoutxpress/index">Past Workouts</a></span>
    </nav>
    <div>
        <div>
            <h3><%=workouts.name %></h3>

            <p>Workout Date: <%=workouts.date %></p>
            <p><%=workouts.description %><br />
            Target Areas:<br /><% if(workouts.upperBody === true){ %>
            Upper Body<br />
            <% } %>
            <% if(workouts.lowerBody === true){ %>
            Lower Body<br />
            <% } %>
            <% if(workouts.cardio === true){ %>
            Cardio<br />
            <% } %>
            <% if(workouts.static === true){ %>
            Static<br />
            <% } %>
            <% if(workouts.weightTraining === true){ %>
            Weight Training
            <% } %>
        </p>
        </div>
        <button type="submit"><a href="/workoutxpress/workouts/<%= workouts.id %>/edit"></a>Edit</button>
        <form action="/workoutxpress/workouts/<%= workouts.id %>?_method=DELETE" method="POST">
            <input type="submit" value="Delete Workout"/>
        </form>
    </div>
</body>
</html>